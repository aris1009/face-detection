import React from 'react';
import Button from '../Button';

function NavigationBar() {
	return (
		<div className="navigation-bar">
			<Button classNamesToChild="pa2">Sign Out</Button>
		</div>
	)
}

export default NavigationBar;